version: "3.7"

services:
  # Payment service (Python)
  payment_service:
    build:
      context: ./
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - 8083:5000
    volumes:
      - .:/app

networks:
  default:
    external:
      name: edms_network
